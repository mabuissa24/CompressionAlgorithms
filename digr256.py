import bitstring as bs
import digrCode as dc

encodedMsg = bs.BitArray()

def digr_encoder(text, code: list):
    """
    Implementation pending completion and testing of CodeDoubleMap class.
    """

    # codeWordLen = dc.bitsRequired(len(code) - 1)
    # i = 0
    # while True:
    #     input = text[i:i+2]
    #     encodeUnigram = True
    #     for j in range(96, len(code)): # check if can encode a digram
    #         if input == code[j]:
    #             encodeUnigram = False
    #             encodedMsg += "0b" + dc.getBin(j)
    #             i += 2
    #             break
    #     if encodeUnigram:
    #         for j in range(96):
                
                
                
            
    